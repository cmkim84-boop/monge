# 몽이는 산책중 — Firebase 연동 설정 가이드

이번에 `index.html`을 고쳐서, 그동안 수동으로 JSON 파일을 다운로드/업로드하던 부분을 Firebase(Firestore)에
자동으로 저장·동기화되도록 바꿨어요. 아이폰에서 산책 기록을 남기면 맥/PC 브라우저에서도 새로고침만 하면
바로 보입니다.

왜 로그인 화면이 추가됐는지: 이 앱은 `github.io` 공개 주소로 배포되기 때문에, 로그인 없이 데이터를 열어두면
산책 시작/종료 GPS 좌표(=집 위치 추정 가능)나 사진이 누구에게나 노출될 수 있어요. 그래서 본인만 아는 이메일/
비밀번호로 로그인해야 데이터를 볼 수 있게 만들었습니다. (Firebase 무료 요금제로 충분하고, 카드 등록도 필요 없어요.)

아래 순서대로 딱 한 번만 설정하면 됩니다.

## 1. Firebase 프로젝트 만들기

1. https://console.firebase.google.com 접속 → 평소 쓰시는 구글 계정으로 로그인
2. "프로젝트 추가" 클릭 → 이름은 자유롭게 (예: `monge-walk`) → Google Analytics는 꺼도 무방 → 프로젝트 만들기

## 2. 웹 앱 등록해서 설정값 받기

1. 프로젝트 개요 화면에서 `</>` (웹) 아이콘 클릭
2. 앱 닉네임 아무거나 입력 (예: `monge-web`) → Firebase Hosting 체크는 안 해도 됨 → 앱 등록
3. 화면에 나오는 `firebaseConfig` 값을 복사해서, 첨부해드린 `firebase-config.js` 파일의
   `apiKey`, `authDomain`, `projectId`, `storageBucket`, `messagingSenderId`, `appId` 자리에 그대로 붙여넣기
   (나중에 "프로젝트 설정 ⚙️ → 일반" 탭에서 언제든 다시 볼 수 있어요)

## 3. 로그인 방식 켜기 (본인 계정 1개만 생성)

1. 왼쪽 메뉴 "Authentication" → "시작하기"
2. 로그인 방법 탭 → "이메일/비밀번호" 선택 → 사용 설정 → 저장
3. "Users" 탭 → "사용자 추가" → 본인이 쓸 이메일/비밀번호 입력 (앱에서 로그인할 때 쓸 계정, 회원가입 화면은 따로 없어요)

## 4. 데이터베이스(Firestore) 만들기

1. 왼쪽 메뉴 "Firestore Database" → "데이터베이스 만들기"
2. 위치는 `asia-northeast3 (서울)` 추천 → 프로덕션 모드로 시작
3. 만들어지면 "규칙" 탭으로 이동해서 아래 내용으로 전체 교체 후 "게시":

```
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /users/{uid}/{document=**} {
      allow read, write: if request.auth != null && request.auth.uid == uid;
    }
  }
}
```

이 규칙은 "로그인한 본인의 uid 아래 데이터만 본인이 읽고 쓸 수 있다"는 뜻이에요. 다른 사람이 주소를 알아도
로그인 못 하면 아무것도 못 봅니다.

## 5. GitHub 저장소에 반영하기

`monge` 저장소(https://github.com/cmkim84-boop/monge)에 아래 3개 파일을 올려주세요.

- `index.html` → 기존 파일 교체 (Firebase 연동 코드 포함)
- `firebase-config.js` → 새 파일 추가 (2번 단계에서 값 채운 것)
- `SETUP.md` → 새 파일 추가 (이 문서, 없어도 동작엔 문제 없음)

GitHub 웹사이트에서 저장소 열고 "Add file → Upload files"로 드래그해서 올리면 됩니다. 커밋하면 GitHub Pages가
자동으로 다시 배포돼요 (보통 1분 이내).

## 6. 확인하기

1. 아이폰 홈 화면의 앱 아이콘으로 실행 (또는 새로고침)
2. 로그인 화면이 뜨면 3번에서 만든 이메일/비밀번호로 로그인
3. 산책 기록해보고, 맥/PC 브라우저에서 같은 주소로 접속해서 같은 계정으로 로그인하면 방금 기록이 그대로 보이는지 확인

## 기존에 로컬(iPhone)에 쌓아둔 산책 기록이 있다면

이번 업데이트로 저장 위치가 브라우저 로컬 저장소 → Firebase로 바뀌기 때문에, 기존 기록은 자동으로 옮겨지지
않아요. 순서대로 하면 그대로 이전됩니다.

1. 업데이트 배포 **전** 화면(기존 버전)에서 "기록 → 다운로드"로 백업 JSON 파일을 미리 저장해두기
2. 5번 순서대로 배포 → 로그인
3. 로그인 후 "기록 → 업로드"에서 1번에서 받은 백업 JSON 파일을 선택하면 Firebase로 그대로 옮겨져요

## 참고

- 사진은 리사이즈해서 Firestore 문서에 함께 저장돼요. 한 산책에 사진을 아주 많이(10장 이상) 찍으면
  문서 용량 제한(1MB)에 걸릴 수 있는데, 그럴 땐 사진을 Firebase Storage로 따로 저장하는 방식으로 바꿔야
  하니 그때 다시 요청해주세요.
- Firebase 무료(Spark) 요금제 기준 하루 읽기 5만/쓰기 2만 건, 저장 용량 1GB까지 무료라 개인 산책 기록
  용도로는 충분합니다. 카드 등록 없이 그대로 쓰면 돼요.
- "기록 → 다운로드"(내보내기) 기능은 그대로 남겨뒀어요. 가끔 수동 백업하고 싶을 때 쓰시면 됩니다.
